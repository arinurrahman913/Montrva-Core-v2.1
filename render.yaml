services:
  - type: web
    name: montrva-dashboard
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/app.py
    envVars:
      - key: FRED_API_KEY
        sync: false
